# WebSocket URL of the InterviewMate backend.
# When using the Vercel-deployed frontend with a local backend,
# ws://localhost still works from HTTPS pages in Chrome/Firefox.
VITE_WS_URL=ws://localhost:8000/ws
